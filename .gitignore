# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Environment secrets — never commit (keep .env.example)
.env
.env.local
.env.*.local
!.env.example
android/local.properties
ios/Flutter/LocalSecrets.xcconfig

# iOS: optional extra secrets (legacy)
ios/Secrets.xcconfig
ios/Runner/Secrets.xcconfig

# Firebase config (project-specific; do not commit)
**/google-services.json
**/GoogleService-Info.plist
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
